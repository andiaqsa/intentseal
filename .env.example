# Optional server-side token for higher public GitHub API rate limits.
# Never prefix server secrets with VITE_.
GITHUB_TOKEN=

# Optional bounded server request timeout (5,000-60,000 ms).
EVIDENCE_TIMEOUT_MS=30000
