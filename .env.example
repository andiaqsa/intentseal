# Optional server-side token for higher public GitHub API rate limits.
# Never expose this token through a VITE_ variable.
GITHUB_TOKEN=

# Optional bounded server request timeout.
# Allowed range: 5,000-60,000 ms.
EVIDENCE_TIMEOUT_MS=30000